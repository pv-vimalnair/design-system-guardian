# Guardian diagnostic sentinels

These fixed assets are the immutable policy's sole styling exception. They are intentionally conspicuous and must include a request ID and policy digest in generated evidence. A sentinel is permitted only after a fresh, available, complete snapshot proves the requested exact identity is missing. It always makes `productionReady=false` and can never be promoted automatically.
