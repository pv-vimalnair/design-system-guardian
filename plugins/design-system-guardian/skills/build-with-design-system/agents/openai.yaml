interface:
  display_name: "Build with Design System"
  short_description: "Build only with approved design assets"
  default_prompt: "When explicitly invoked, use $build-with-design-system to check setup, ask permission only for the exact local change, and build from exact approved bindings. Do not claim this prompt creates an always-on protected route."
